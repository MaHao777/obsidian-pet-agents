{
  "name": "obs-petagents",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests\\memory-rules.test.ts tests\\runtime-policies.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.10",
    "obsidian": "^1.8.10",
    "typescript": "^5.9.2"
  }
}
